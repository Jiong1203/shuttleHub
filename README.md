# 🏸 ShuttleHub - 羽球開團與報名平台

ShuttleHub 是一個專為羽球愛好者設計的活動開團與報名網站。無論您是想尋找球友切磋球技，還是熱心的主辦者想要舉辦活動，ShuttleHub 都能滿足您的需求。

## ✨ 核心功能

- **🏠 首頁 (Landing Page)**
  - 吸引人的 Hero 區塊與功能亮點介紹
  - 快速引導使用者尋找活動或開團

- **📋 活動瀏覽**
  - 查看所有即將到來的羽球活動
  - 卡片式設計，清晰呈現時間、地點、程度與費用等關鍵資訊

- **🏸 我要開團**
  - 簡單直覺的表單，快速建立新活動
  - 支援設定程度分級 (新手友善、中級程度等)、人數上限與費用

- **📝 報名系統**
  - 使用者可查看活動詳情並一鍵報名
  - 即時顯示目前報名狀況 (已報名人數/名額上限)
  - 自動候補功能（人數超過上限時）

- **👥 管理介面 (主辦者專用)**
  - 管理自己發布的活動
  - 編輯、刪除活動
  - 檢視報名統計數據

## 🛠️ 技術堆疊

### 前端

- **框架**: [Vue 3](https://vuejs.org/) (Script Setup)
- **建置工具**: [Vite](https://vitejs.dev/)
- **狀態管理**: [Pinia](https://pinia.vuejs.org/)
- **路由管理**: [Vue Router](https://router.vuejs.org/)
- **樣式**: Vanilla CSS (搭配 CSS Variables 實作設計系統)
- **語言**: TypeScript

### 後端

- **執行環境**: Node.js
- **框架**: Express.js
- **資料庫**: PostgreSQL (Supabase)
- **ORM**: Prisma
- **認證**: JWT (JSON Web Token)

## 🚀 快速開始

### 環境需求

- Node.js (建議 v18+)
- npm

### 步驟一：安裝依賴

#### 1. 安裝前端依賴

```sh
npm install
```

#### 2. 安裝後端依賴

```sh
cd backend
npm install
```

### 步驟二：設定環境變數

#### 本地開發環境

在專案根目錄創建 `.env` 文件（如果不存在）：

```env
VITE_API_URL=http://localhost:3001/api
```

#### 生產環境（Vercel 部署）

在 Vercel 專案設置中添加環境變數：

1. 進入 Vercel 專案 Dashboard
2. 點擊 **Settings** → **Environment Variables**
3. 添加以下環境變數：
   - **Name**: `VITE_API_URL`
   - **Value**: 您的後端 API URL（例如：`https://your-backend-api.vercel.app/api` 或 `https://api.yourdomain.com/api`）
   - **Environment**: 選擇 `Production`、`Preview` 和 `Development`（根據需要）

> **注意**: 
> - 前端環境變數必須以 `VITE_` 開頭才能在 Vite 中使用
> - 後端已配置 Supabase 雲端資料庫，`.env` 檔案已包含連線資訊
> - 後端的 `.env` 檔案已被 `.gitignore` 忽略，如需查看配置請參考 `backend/.env.example`

### 步驟三：啟動專案

#### 1. 啟動後端伺服器

在 `backend` 目錄下執行：

```sh
npm run dev
```

後端伺服器將運行於 `http://localhost:3001`

#### 2. 啟動前端開發伺服器

回到專案根目錄執行：

```sh
npm run dev
```

前端應用將運行於 `http://localhost:5173`

### 步驟四：開始使用

開啟瀏覽器訪問 `http://localhost:5173`，使用以下測試帳號登入：

#### 測試帳號

| 角色   | Email                | 密碼         | 權限                   |
| ------ | -------------------- | ------------ | ---------------------- |
| 管理員 | admin@shuttlehub.com | admin123     | 完整管理權限           |
| 團長   | organizer@test.com   | organizer123 | 可建立、編輯、刪除活動 |
| 會員   | member@test.com      | member123    | 可報名活動             |

## 📂 專案結構

```
shuttleHub/
├── backend/                 # 後端專案
│   ├── src/
│   │   ├── controllers/    # 控制器
│   │   ├── middleware/     # 中介層（認證、授權）
│   │   ├── routes/         # API 路由
│   │   ├── services/       # 業務邏輯
│   │   ├── types/          # 型別定義
│   │   ├── utils/          # 工具函式
│   │   └── index.ts        # 入口檔案
│   ├── prisma/
│   │   ├── schema.prisma   # 資料庫 Schema
│   │   └── seed.ts         # 測試資料
│   └── .env                # 環境變數
├── src/                     # 前端專案
│   ├── views/              # 頁面視圖
│   ├── components/         # UI 元件
│   ├── stores/             # Pinia 狀態管理
│   ├── services/           # API 服務
│   ├── assets/             # 靜態資源
│   └── router/             # 路由配置
└── doc/                     # 專案文件
```

## 🔧 其他指令

### 前端

```sh
# 建置生產版本
npm run build

# 預覽生產版本
npm run preview

# 程式碼檢查
npm run lint

# 程式碼格式化
npm run format
```

### 後端

```sh
# 建置生產版本
npm run build

# 執行生產版本
npm run start

# Prisma 相關
npx prisma studio          # 開啟資料庫管理介面
npx prisma migrate dev     # 執行資料庫遷移
npx ts-node prisma/seed.ts # 重新建立測試資料
```

## 📝 API 文件

### 認證 API

- `POST /api/auth/register` - 註冊新使用者
- `POST /api/auth/login` - 使用者登入

### 活動 API

- `GET /api/events` - 取得活動列表
- `GET /api/events/:id` - 取得活動詳情
- `POST /api/events` - 建立活動 🔒
- `PUT /api/events/:id` - 更新活動 🔒
- `DELETE /api/events/:id` - 刪除活動 🔒

### 報名 API

- `POST /api/events/:eventId/register` - 報名活動 🔒
- `DELETE /api/registrations/:id` - 取消報名 🔒
- `GET /api/registrations/me` - 查詢我的報名 🔒

🔒 = 需要登入認證

## � 開發文件

- [功能展示與操作流程](./doc/walkthrough.md)
- [後端設計提案](./backend-design-proposal.md)
- [開發狀態總覽](./DEVELOPMENT_STATUS.md)

## 🎯 功能特色

- ✅ 完整的使用者認證系統（JWT）
- ✅ 角色權限管理（Admin / Organizer / Member）
- ✅ 活動 CRUD 操作
- ✅ 報名系統與候補機制
- ✅ 響應式設計，支援各種裝置
- ✅ 雲端資料庫（Supabase）

## 🤝 貢獻

歡迎提交 Issue 或 Pull Request！

## 📄 授權

MIT License
